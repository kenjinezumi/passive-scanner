[package]
name = "passive-scanner"
version = "0.1.0"
edition = "2018"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
reqwest = { version = "0.11", features = ["blocking", "json"] }

[dependencies.rocket]
version = "0.5.0-rc.3"
features = ["json"]